#include<QApplication>
#include<QLabel>

int main(int argc, char* argv[])
{
    QApplication app(argc, argv);
    QLabel *label=new QLabel();
    label->show();
    label->setWindowTitle("Xml To Jason Converter");
    label->resize(600,600);
    return app.exec();

}

